@model IEnumerable<LIC_CMS.Areas.Admin.Models.PlanModel>

<table id="table_data" class="table table-hover table-bordered">
    <thead>
        <tr>
            @*<th>Plan Id</th>*@
            <th>Plan Name</th>
            <th>Plan Code</th>
            <th>Action</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                @*<td>@item.PLAN_ID</td>*@
                <td>@item.PLAN_NAME</td>
                <td>@item.PLAN_CODE</td>
                <td><a><span onclick="fillEditForm(@item.PLAN_ID)" data-toggle="modal" data-target="#myPlanModal" class="glyphicon glyphicon-edit cursorPointer"></span></a></td>
                @*<td>@Html.ActionLink("Delete", "Delete", new { id = item.PLAN_ID }, new { onclick = "return confirm('Are you delete this Plan?')" })</td>*@
                <td><a href="Delete/@item.PLAN_ID" onclick="return confirm('Are you delete this Plan?')"><span class="glyphicon glyphicon-trash"></span></a></td>
            </tr>
        }
    </tbody>
</table>

<script src="~/Content/datatables/jquery.dataTables.min.js"></script>
<script src="~/Content/datatables/dataTables.bootstrap.min.js"></script>

<script>
    $(document).ready(function () {
        $('#table_data').DataTable({
            responsive: true
        });
    });
</script>



